<tr class="cohortLink js-cohortIndex" data-link="<%= cohort_path(cohort) %>">
  <td><%= cohort.name %></td>
  <td><%= cohort.start_at.strftime("%B %d, %Y") %></td>
  <td><%= cohort.end_at.strftime("%B %d, %Y") %></td>
</tr>
